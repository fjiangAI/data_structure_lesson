name: Course CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3

      - name: Install C compiler
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: Check course structure
        run: python3 tools/check_course_structure.py

      - name: Check local links
        run: python3 tools/check_links.py

      - name: Check text encoding
        run: python3 tools/check_encoding.py

      - name: Compile all C examples
        run: python3 tools/compile_examples.py --require-compiler

      - name: Run lab starter public tests
        run: python3 tools/run_lab_tests.py --all --expect-starter-fail

      - name: Run lab reference public tests
        run: python3 tools/run_reference_solutions.py

      - name: Run sanitizer checks for C examples
        run: python3 tools/sanitize_examples.py

      - name: Check Pages artifact contents
        run: python3 tools/check_pages_artifact.py

