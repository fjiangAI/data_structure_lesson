# Week 03 线性表之二：链表 练习题

## A. 基础必做：概念与结构

1. 用不超过 120 字说明本周结构保存了什么“对象”和什么“关系”。
2. 画出一个包含 5 个元素的小例子，标出关键字段、指针、下标、父子关系或邻接关系。
3. 写出本周最核心 ADT 的 C 风格接口，至少包含初始化、核心操作、销毁或清理。
4. 解释本周结构在课程主线中的位置：它相对于上一类结构增加了什么表达能力，付出了什么代价？

## B. 手推必做：操作过程

5. 手工执行 `头插`，逐步写出每一步读了哪些字段、写了哪些字段。
6. 对 `尾插` 给出正常路径和失败路径各一个例子。
7. 选择一个操作，写出“操作前不变量、操作步骤、操作后不变量仍成立”的三段式说明。

## C. 编程必做：代码阅读与测试

8. 阅读 [examples/linked_list.c](examples/linked_list.c)，指出结构体定义、初始化函数、核心修改函数和输出函数。
9. 给示例代码补 3 个测试：正常情况、边界情况、错误输入或失败路径。
10. 找出示例代码中最依赖 C 语言细节的一处，例如指针、数组下标、动态内存或字符串结束符。

## D. 复杂度与证明（挑战）

11. 对下列操作分别说明输入规模和复杂度来源：

    - 头插：O(1)，关键动作：新结点指向原首元结点，头结点指向新结点
    - 尾插：O(1) 或 O(n)，关键动作：若维护尾指针可直接追加
    - 按位查找：O(n)，关键动作：从头逐步走到第 i 个结点
    - 已知前驱插入：O(1)，关键动作：修改两条 next 指针
    - 逆置：O(n)，关键动作：逐结点改变 next 方向

12. 构造一个最坏情况输入，并说明它为什么触发最坏复杂度。
13. 如果输入规模扩大 100 倍，本周结构最先暴露的瓶颈是什么？

## E. 错误代码诊断（必做）

下面这段代码很像 LLM 生成的简化实现，请指出 bug、给出触发 bug 的输入，并写出修正思路。

```c
p->next = s; s->next = p->next;
```

诊断要求：

1. 说明它破坏了哪个结构不变量。
2. 写一个最小反例。
3. 给出修正后的关键语句或伪代码。

## F. 本周专属训练

1. 手推头插、尾插、按值删除
2. 写出逆置链表的三指针含义
3. 让 LLM 生成链表删除函数并设计首结点删除测试

## G. 设计与 AI 审查（拓展）

1. 让 LLM 写单链表插入和删除代码，重点审查 `s->next` 与 `p->next` 的赋值顺序。
2. 构造一个会丢失后继结点或断链的错误实现，并画出执行前、中、后的指针图。
3. 比较带头结点和不带头结点两种接口在删除首元结点时的差异。
4. 提交证据：最小反例链表、错误赋值顺序、修正理由和释放内存说明。
