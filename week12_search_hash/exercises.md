# Week 12 查找结构：顺序、二分与哈希表 练习题

## A. 基础必做：概念与结构

1. 用不超过 120 字说明本周结构保存了什么“对象”和什么“关系”。
2. 画出一个包含 5 个元素的小例子，标出关键字段、指针、下标、父子关系或邻接关系。
3. 写出本周最核心 ADT 的 C 风格接口，至少包含初始化、核心操作、销毁或清理。
4. 解释本周结构在课程主线中的位置：它相对于上一类结构增加了什么表达能力，付出了什么代价？

## B. 手推必做：操作过程

5. 手工执行 `顺序查找`，逐步写出每一步读了哪些字段、写了哪些字段。
6. 对 `二分查找` 给出正常路径和失败路径各一个例子。
7. 选择一个操作，写出“操作前不变量、操作步骤、操作后不变量仍成立”的三段式说明。

## C. 编程必做：代码阅读与测试

8. 阅读 [examples/search_hash.c](examples/search_hash.c)，指出结构体定义、初始化函数、核心修改函数和输出函数。
9. 给示例代码补 3 个测试：正常情况、边界情况、错误输入或失败路径。
10. 找出示例代码中最依赖 C 语言细节的一处，例如指针、数组下标、动态内存或字符串结束符。

## D. 复杂度与证明（挑战）

11. 对下列操作分别说明输入规模和复杂度来源：

    - 顺序查找：O(n)，关键动作：逐个比较关键字
    - 二分查找：O(log n)，关键动作：在有序数组中折半
    - 哈希插入：平均 O(1)，关键动作：计算地址，冲突时探测
    - 哈希查找：平均 O(1)，关键动作：沿同一探测序列查找
    - 再散列：O(n)，关键动作：扩大表并重新插入

12. 构造一个最坏情况输入，并说明它为什么触发最坏复杂度。
13. 如果输入规模扩大 100 倍，本周结构最先暴露的瓶颈是什么？

## E. 错误代码诊断（必做）

下面这段代码很像 LLM 生成的简化实现，请指出 bug、给出触发 bug 的输入，并写出修正思路。

```c
if (table[i].state != OCCUPIED) return -1;
```

诊断要求：

1. 说明它破坏了哪个结构不变量。
2. 写一个最小反例。
3. 给出修正后的关键语句或伪代码。

## F. 本周专属训练

1. 手推 key%11 的线性探测插入
2. 构造二分查找失败例子
3. 让 LLM 解释哈希删除并检查删除标记

## G. 设计与 AI 审查（拓展）

1. 设计开放定址哈希表删除场景，说明为什么不能简单把槽位置空，而需要删除标记或重建探测链。
2. 让 LLM 写线性探测查找/删除代码，检查它是否区分空槽、已删除槽和有效槽。
3. 统计不同装载因子下查找成功和失败的平均探测次数，并解释趋势。
4. 提交证据：探测链图、错误删除反例、装载因子实验表。
